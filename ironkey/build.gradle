
ospackage {
    packageName = 'ironkey'
    version = '2.2.0'
    release = '1'
    os = LINUX

    requires('bindfs')
}

buildRpm {
    requires('glibc(x86-32)')
    requires('libgcc_s.so.1')
}

buildDeb {
    requires('lib32ncurses5')
    requires('lib32z1')
    requires('lib32gcc1')
}

bintray {
    pkg {
        name = ospackage.packageName
        version {
            name = ospackage.version
            vcsTag = ospackage.version
            attributes = [:]
        }
    }
}
