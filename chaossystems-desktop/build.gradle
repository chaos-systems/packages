ospackage {
    packageName = 'chaossystems-desktop'
    version = '1.1.1'
    release = '1'
    os = LINUX

    requires('code-insiders')
    requires('chromium-browser')
    requires('firefox')
    requires('kitty')
    requires('slack-desktop')
}

buildRpm {
}

buildDeb {
}

bintray {
    pkg {
        name = ospackage.packageName
        version {
            name = ospackage.version
            vcsTag = ospackage.version
            attributes = [:]
        }
    }
}
